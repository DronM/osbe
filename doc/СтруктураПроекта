build(f)
Сборка проекта
Создается при создании проекта.

	production(f)
	????Создается при создании проекта.????
	
	sql(f)
	Все скрипты sql.
	Создается при создании проекта.
	Копируется шаблон для создания бд???
	
	
	templates(f)
	Создается при создании проекта.
		
		controllers(f)
		Создается при создании проекта.
		Создаются следующие ссылки
		Controller_js.tmpl
		Controller_php.tmpl
		Какие то еще общие контроллеры (Constant,User,FileStore)
		
		models(f)
		Создается при создании проекта.
		Создаются следующие ссылки
		MainMenu_Model_php.tmpl
		Model_php.tmpl
		Какие то еще общие модели (ConstantList_Model_php.xsl)
		
		
		js(f)
		Создается при создании проекта.
		Файл ChildForm_js.xsl создается из шаблона FRAME_WORK_PATH.'build/templates/js/'.ChildForm_js.tmpl
		
		
		views(f)
		Создается при создании проекта.
		Создание копий файлов из шаблонов tmpl
		
		
		

	index.php
	Ссылка на файл FRAME_WORK_PATH.'build/'.index.php
	Создается при создании проекта.
	
	metadata.xml
	Метаданные проекта.
	Создается при создании проекта.
	metadata.xml.tmpl
	

controllers(f)
Собранные контроллеры. Собираются автоматически.
Создается при создании проекта.

css(f)
Все стили проекта.
Создается при создании проекта.
Копируются ссылки на все стили из FRAME_WORK_PATH.'build/templates/css/*.css'

doc(f)
Описание объектов проекта.
Создается при создании проекта.
Создается ссылка на файл FRAME_WORK_PATH.'build/templates/css/'.style.css

functions(f)
Все прочие классы, функции.
Создается при создании проекта.
Шаблон db_con.php создается из шаблона FRAME_WORK_PATH.'build/templates/functions/'.db_con.php.tmpl

img(f)
Все картинки проекта
Создается при создании проекта.

js(f)
Создается при создании проекта.

models(f)
Все модели проекта.
Создается при создании проекта.

views(f)
Все view.xslt стили, которые автоматом бурутся при v=ViewXMLHSLT teml=имя_файла.
Создается при создании проекта.


Config.php
Все основные константы
Создается из шаблона FRAME_WORK_PATH.'build/templates/'.Config.php.tmpl при создании проекта


Config.unique.php
Создается из шаблона FRAME_WORK_PATH.'build/templates/'.Config.unique.php.tmpl  при создании проекта

version.php
Создается из шаблона FRAME_WORK_PATH.'build/templates/'.version.php.tmpl  при создании проекта
