/* Copyright (c) 2012 	Andrey Mikhalevich, Katren ltd.*//*		Description*///ф/** Requirements * @requires common/functions.js*//* constructor */function GridRow(id,options){	options = options || {};	options.tagName = options.tagName || this.DEF_TAG_NAME;		GridRow.superclass.constructor.call(this,id,options.tagName,options);	}extend(GridRow,ControlContainer);GridRow.prototype.DEF_TAG_NAME = "tr";GridRow.prototype.m_initCells;GridRow.prototype.setInitColumnOrder = function(){	this.m_initCells = {};	for (var id in this.m_elements){		this.m_initCells[id] = clone(this.m_elements[id]);	}}GridRow.prototype.setColumnOrder = function(orderArray){		this.m_elements = {};		for (var i=0;i<orderArray.length;i++){		if (!orderArray[i].checked)continue;				for (var id in this.m_initCells){			if (this.m_initCells[id].getName()==orderArray[i].id){				this.m_elements[id] = this.m_initCells[id];				break;			}		}	}		for (var id in this.m_initCells){		if (!this.m_initCells[id].getVisible() && !orderArray[this.m_initCells[id].getName()]){						this.m_elements[id] = this.m_initCells[id];		}	}}/*GridRow.prototype.setElementById = function(id,cell){	this.m_initCells[id] = clone(cell);	GridRow.superclass.setElementById.call(this,id,cell);}*/